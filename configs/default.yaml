# configs/default.yaml
project: "CISC_473_project"
seed: 42

paths:
  repo_dir: "/home/colin/projects/CISC_473_project"
  data_root: "/home/colin/projects/CISC_473_project_data"
  # Ideally i rememberd to change this back to google drive version before push
  # data_root: "/content/drive/MyDrive/projects/CISC_473_project_data"
  # repo_dir: "/content/drive/MyDrive/projects/CISC_473_project/"
  out_dir: "/home/colin/projects/CISC_473_project/outputs/run1"
  save_dir: "/home/colin/projects/CISC_473_project/checkpoints/run1"

data:
  patch_size: 128
  batch_size: 16
  noise_sigma: 25

train:
  epochs: 50
  lr: 3e-4
  optimizer: "adamw"
  weight_decay: 0.01
  save_every: 5
